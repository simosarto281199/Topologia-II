\ProvidesPackage{topology-2}[2022/02/10 - Style file for notes of Topology II]

\usepackage[cache version =1, number in = section]{fancythm}
\usepackage{mkessler-enumerate}
\usepackage{mkessler-math}
\usepackage{mkessler-todo}
\usepackage{mkessler-hypersetup}
\usepackage{mkessler-vocab}
\usepackage[fancyhead]{mkessler-lectures}

\usepackage{mkessler-incfig}
\usepackage{mkessler-counters}
\usepackage{mkessler-enumerate}
\usepackage{csquotes}
\usepackage{import}

\NewFancyTheorem[thmtools={style=thmblackmargin}, group=small, name = Editor's note]{editor}

\def\blank{-}
\def\dash{-}
\let\chaindimension\abs
\let\tensor\otimes
\let\chaintensor\tensor
\let\directsum\bigoplus



% This should normally be less hackier, but mkessler-refproof seems to be bugged
\let\oldproofstart \proof
\let\oldproofend   \endproof
\usepackage{mkessler-refproof}
\renewenvironment{oldproof}{\oldproofstart}{\oldproofend}
\usepackage{mkessler-refproof}

\NewDocumentEnvironment{well-definedness}{}
  {%
    \begin{oldproof}[Well-definedness]%
  }%
  {%
    \end{oldproof}%
  }%

\NewDocumentCommand\set{m}
  {
    \left\{ #1 \right\}
  }


\newcommand\connectingmark[1][Z]{\arrow[phantom,d,""{coordinate, name=#1}]}
\newcommand\connectingmap[2][Z]
  {
    \arrow
    [
    dll,
    "#2",
    rounded corners,
    to path =
      {
        -- ([xshift=2em]\tikztostart.east)
        |- (#1) [near end]\tikztonodes
        -| ([xshift=-2ex]\tikztotarget.west)
        -- (\tikztotarget)
      }
    ]
  }
\newcommand\personal{\todo[color=gray!5]}

\NewDocumentCommand\Csing{s}
{
  \IfBooleanTF{#1}
  {
    C^{\chainbullet}
  }
  {
    C_{\chainbullet}
  }
}

\AddProvidedFancyTheoremToGroup{remark}{big}
